<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MunchBotLight" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="Theme.MunchBot.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_splash_bg</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.MunchBot" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomDatePickerDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:colorAccent">@color/circle_color_selected</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

</resources>